from elixir_dcp.scripts.db import START_UP_DATA


class Config(object):

    # name of the application
    TITLE = 'ELIXIR-LU Data Submission System'
    SECRET_KEY = b'deneme'
    SQLALCHEMY_DATABASE_URI = "sqlite:////home/elixirdcp/app-data/elixir-dcp.db"
    SQLALCHEMY_TRACK_MODIFICATIONS = False

    UPLOAD_FOLDER = '/home/elixirdcp/app-data'
    UPLOADS_SERVER_PATH = '/attachments'

    DATA_STEWARDS_MAILS = ['pinar.alper@uni.lu']

    # Menu Bar Items a list of  (Name, Link, Sub item list, role)
    APP_MENU_BAR_ITEMS = [
        ('Admin', None, [('Submissions', 'list_submissions'), ('Users', 'list_users')], 'admin'),
        ('My Submissions', 'list_my_submissions', [], 'data_provider'),
    ]
    DATA_INIT = START_UP_DATA

    # OIDC config
    OIDC_CLIENT_SECRETS = 'client_secrets.json'
    OIDC_SCOPES = ['openid', 'email', 'profile', 'bona_fide_status', 'groupNames']
    OIDC_COOKIE_SECURE = False

class ProdConfig(Config):
    DEBUG = False
    SQLALCHEMY_ECHO = True

    AUTHENTICATION_METHOD = 'AAI'
    OIDC_COOKIE_SECURE = True

    #Flask-Mail config
    MAIL_SERVER = 'localhost'
    MAIL_PORT = 25
    MAIL_USE_TLS = False
    MAIL_USE_SSL = False



class DevConfig(Config):
    DEBUG = False
    SQLALCHEMY_ECHO = True


    AUTHENTICATION_METHOD = 'AAI'  # Possible methods is 'AAI' or 'CONFIG'


    #Flask-Mail config
    MAIL_SERVER = 'smtp.gmail.com'
    MAIL_PORT = 465
    MAIL_USE_SSL = True
    MAIL_USE_TLS = False
    MAIL_USERNAME = 'pinarpink@googlemail.com'
    MAIL_PASSWORD = 'Baloncuklar_06'




class TestConfig(Config):
    DEBUG = True
    SQLALCHEMY_ECHO = False
    SQLALCHEMY_DATABASE_URI = "sqlite:///test-elixir-dcp.db"

    AUTHENTICATION_METHOD = 'CONFIG'  # Possible methods is 'AAI' or 'CONFIG'
    # a dict containing user and password items for CONFIG based authentication
    AUTHENTICATION_DICT = {'pinar.alper@uni.lu': 'palper', 'pi@some.uni': 'user'}

    ASSETS_DEBUG = True
    TESTING = True
    CACHE_CONFIG = {'CACHE_TYPE': 'null'}
    WTF_CSRF_ENABLED = False
